name: Post welcome comment
on:
  workflow_dispatch:
  pull_request:
    types: [opened]
permissions:
  pull-requests: write
jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      -  name: first step
         run: echo "This is my first step" 
